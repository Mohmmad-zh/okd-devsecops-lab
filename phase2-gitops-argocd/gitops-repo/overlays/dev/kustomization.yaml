apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aspnetapp-dev

resources:
  - ../../base
  - namespace.yaml

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: aspnetapp
